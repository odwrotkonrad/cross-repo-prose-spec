# Feature: Example Greeter

<!-- [>] 🤖🤖 -->

Scenario: a caller with no argument still gets a valid greeting instead of an error
  Status: todo
  When I invoke `greet` with no argument
  Then the output reads `hello world`

<!-- [<] 🤖🤖 -->
