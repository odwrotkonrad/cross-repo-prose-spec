# Feature: llm-git-mr-suggest.zsh

<!-- [>] 🤖🤖 -->

## As a reviewer

Reads the generated MR description, does not read the diff first.

### A big MR description stays navigable (implemented)

I want a `### <scope>` area beyond roughly 6 bullets split into
`#### <feature>` subsections, a flat base description regrouped with bullet
wording kept, small areas left flat,
so that a large change reads by feature instead of as one list.

<!-- [<] 🤖🤖 -->
